import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.slotarcade.gemreels',
  appName: 'Gem Reels',
  // Vite builds the web app into dist/; Capacitor packages this directory.
  webDir: 'dist',
};

export default config;
